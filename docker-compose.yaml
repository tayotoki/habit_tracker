services:
  db:
    image: postgres:16.1-alpine
    volumes:
      - postgresdata:/var/lib/postgresql/data
    env_file:
      - .env
    environment:
      POSTGRES_NAME: $POSTGRES_NAME
      POSTGRES_USER: $POSTGRES_USER
      POSTGRES_PASSWORD: $POSTGRES_PASSWORD
      POSTGRES_PORT: $POSTGRES_PORT
    ports:
      - "5432:5432"

  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
      - redis

  redis:
    image: redis:6.2-alpine
    volumes:
      - redisdata:/data

#  celery:
#    build: .
#    command: celery -A config worker -l info -E
#    volumes:
#      - .:/usr/src/app/
#    depends_on:
#      - redis
#
#  celery-beat:
#    build: .
#    command: celery -A config worker -l info -E -B -Q habits_periodic
#    volumes:
#      - .:/usr/src/app/
#    depends_on:
#      - redis


volumes:
  postgresdata:
  redisdata:
